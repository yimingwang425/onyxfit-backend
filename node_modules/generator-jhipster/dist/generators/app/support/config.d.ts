/**
 * Load common options to be stored.
 * @deprecated
 */
export declare function loadStoredAppOptions(this: any, { options, jhipsterConfig, log }?: {
    options?: any;
    jhipsterConfig?: any;
    log?: any;
}): void;
/**
 * Load app configs into application.
 * all variables should be set to dest,
 * all variables should be referred from config,
 * @param {any} config - config to load config from
 * @param {any} dest - destination context to use default is context
 */
export declare const loadAppConfig: ({ config, application, useVersionPlaceholders, }: {
    config: any;
    application: any;
    useVersionPlaceholders?: boolean;
}) => void;
/**
 * @param {Object} dest - destination context to use default is context
 */
export declare const loadDerivedAppConfig: ({ application }: {
    application: any;
}) => void;
